# Results

This directory contains compact, finalized tables and reports whose source runs are named
in each study. Large checkpoints, raw logs, and generated sequence collections belong in
external artifact storage or ignored local run directories.

