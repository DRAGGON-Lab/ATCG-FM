[project]
name = "atcg-sequence"
version = "0.1.0"
description = "Sequence and dataset primitives for ATCG-FM"
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atcg"]
