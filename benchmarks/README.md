# Benchmarks

Operator benchmarks record shapes, dtype, device, warmup, synchronization policy, forward
and backward timing, and peak memory. Performance results do not substitute for numerical,
gradient, or model-quality validation.

